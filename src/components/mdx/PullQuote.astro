---
interface Props {
  attribution?: string;
}
const { attribution } = Astro.props;
---
<blockquote class="pullquote">
  <slot />
  {attribution && <span class="attr">— {attribution}</span>}
</blockquote>
