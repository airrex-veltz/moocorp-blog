---
interface Props {
  cols?: 2 | 3 | 4;
}
const { cols = 3 } = Astro.props;
const colsStyle = `grid-template-columns: repeat(${cols}, 1fr);`;
---
<div class="statgrid" style={colsStyle}>
  <slot />
</div>
