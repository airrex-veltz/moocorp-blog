---
import Base from './Base.astro';
import PostMeta from '../components/PostMeta.astro';
import type { CollectionEntry } from 'astro:content';

interface Props {
  post: CollectionEntry<'posts'>;
}

const { post } = Astro.props;
const { data, body } = post;

const wordCount = body ? body.replace(/\s+/g, '').length : 0;
const readingTime = Math.max(1, Math.round(wordCount / 500));
---
<Base
  title={data.title}
  description={data.description}
  language={data.language}
  publishedAt={data.publishedAt}
  updatedAt={data.updatedAt}
  schemaType="Article"
  tickers={data.tickers}
  tags={data.tags}
>
  <article>
    <header class="mb-10 pb-8 border-b border-ink">
      <h1 class="text-3xl sm:text-4xl md:text-5xl font-extrabold tracking-tight leading-[1.15] mb-4">
        {data.title}
      </h1>
      <p class="text-lg text-ink-subtle leading-relaxed mb-5">{data.description}</p>
      <PostMeta
        market={data.market}
        tickers={data.tickers}
        publishedAt={data.publishedAt}
        language={data.language}
        readingTime={readingTime}
      />
    </header>

    <div class="ad-slot ad-slot-top"></div>

    <div class="prose prose-ink prose-article max-w-none">
      <slot />
    </div>

    <div class="ad-slot ad-slot-bottom"></div>

    {data.tags.length > 0 && (
      <footer class="mt-12 pt-8 border-t border-ink">
        <div class="text-xs uppercase tracking-widest text-ink-muted mb-3">관련 태그</div>
        <div class="flex flex-wrap gap-2">
          {data.tags.map((tag) => (
            <a href={`/tags/${tag}`} class="pill-subtle hover:border-ink hover:text-ink transition-colors">
              <span>#{tag}</span>
            </a>
          ))}
        </div>
      </footer>
    )}

    <nav class="mt-12 pt-6 border-t border-ink-line">
      <a href="/" class="inline-flex items-center gap-2 text-sm hover:underline underline-offset-4">
        <span class="ms" aria-hidden="true">arrow_back</span>
        <span>다른 분석 보기</span>
      </a>
    </nav>
  </article>
</Base>
